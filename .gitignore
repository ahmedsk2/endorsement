*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Claude Code personal overrides (this machine only)
.claude/settings.local.json
storage/backups/

# Playwright e2e run artefacts (churn every `npm run test:e2e`; config uses the
# default outputDir and the `list` reporter, so this is the only output path)
/test-results/
